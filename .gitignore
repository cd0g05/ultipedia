# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Node (the frontend has its own, but a stray root install must never be staged)
node_modules/

# Virtual environments
.venv

# IDE / OS
.idea/
.DS_Store

# Secrets / local env
.env
.env.local
*.env.local

# Local-only Claude config (skills symlink into .cicadas-skill/)
.claude/

# Cicadas state (local to the main checkout by design)
#.cicadas/

# Cicadas per-worktree helpers (regenerated per branch)
context.md
#.cicadas-skill
